# IP Address Detection

This content has been consolidated into
[NETWORK_CONFIGURATION.md](NETWORK_CONFIGURATION.md).

See the "How IP Detection Works" and "Detection by Deployment Method" sections
of the [Network Configuration & IP Detection](NETWORK_CONFIGURATION.md) guide
for:
- Frontend detection priority (manual override, backend host, WebRTC, fallback)
- Private IP detection algorithm
- WebRTC detection process
- Per-deployment detection behavior (local, Docker, remote server)
- Troubleshooting (wrong IP, placeholder, container IP)
