apiVersion: v1
kind: ConfigMap
metadata:
  name: licensing-config
  namespace: madhyamas-licensing
  labels:
    app.kubernetes.io/name: madhyamas-licensing
    app.kubernetes.io/part-of: madhyamas
data:
  # Non-secret configuration for the licensing server.
  PORT: "8080"
  BIND_ADDR: "0.0.0.0"
  RUST_LOG: "info"
